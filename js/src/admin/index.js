import { extend } from 'flarum/common/extend';
import app from 'flarum/admin/app';

// Admin panel customization (minimal)
app.initializers.add('hadi-flarum-theme-modern', function () {
  // Add theme settings if needed
});